import { getHostnameTemplate } from "./getHostnameTemplate";
export var getResolvedHostname = function (resolvedRegion, _a) {
    var signingService = _a.signingService, regionHostname = _a.regionHostname, partitionHostname = _a.partitionHostname;
    return regionHostname !== null && regionHostname !== void 0 ? regionHostname : getHostnameTemplate(signingService, { partitionHostname: partitionHostname }).replace("{region}", resolvedRegion);
};
